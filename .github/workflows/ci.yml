name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.21.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright browser
        # Needed by both the vitest browser-mode unit tests and the e2e tests.
        # No --with-deps: the ubuntu-latest runner already ships chromium's libs,
        # and the apt step it triggers was hanging the job.
        run: pnpm exec playwright install chromium

      - name: Type check
        run: pnpm run check

      - name: Lint
        run: pnpm run lint

      - name: Unit tests
        run: pnpm run test:unit -- --run

      - name: E2E tests
        run: pnpm run test:e2e
